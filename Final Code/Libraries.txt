You need the following libraries installed in your Arduino IDE:

SD.h:
Handles SD card operations.
Install via Arduino Library Manager.
Teensy Core Libraries:
Automatically installed with Teensyduino.


Upload the main.ino to your Teensy.
Place the config.h file in the same folder as main.ino.
Insert a formatted SD card into the Teensy’s MicroSD slot.
Run the system and verify data logging in the log.csv file.